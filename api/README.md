# API Definitions

This directory contains OpenAPI specifications for the platform's management APIs and protocol schemas:

* **Edge Gateway Admin API** — REST endpoints for configuring routing rules, tenant policies, and viewing service discovery status.
* **Wire Protocol Schema** — Binary framing format used for HTTP-over-libp2p tunneling (length-prefixed frames with streaming support).
