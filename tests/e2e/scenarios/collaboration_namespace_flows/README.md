# collaboration_namespace_flows

Deterministic collaboration-namespace regression gate.

Covers:
- namespace member invite can discover and connect by name
- viewer invite can list but cannot connect
- share invite still works cross-scope without namespace membership
