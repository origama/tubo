# [PR #68] Issue 5 — Remove public legacy role UX

Linked PR: https://github.com/origama/tubo/pull/68

Labels: enhancement, area:cli, area:docs, breaking-compat, prio:medium

## Goal
Keep only intent-based commands in public CLI surface.

## Scope
- reject `tubo edge run`, `tubo service run`, `tubo bridge run`
- keep `tubo relay` intent command
- keep runtime internals unchanged
- add `tubo attach <name> --port <port>` shorthand
- update docs + tests

## Acceptance
- legacy role commands (except relay intent command) are rejected with help text
- shorthand attach path works
- docs reflect new UX
