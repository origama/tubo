# [PR #68] Issue 1 — Trust store embedded per signed bundles

Linked PR: https://github.com/origama/tubo/pull/68

Labels: enhancement, security, area:cli, area:protocol, prio:high

## Goal
Add `internal/trust` with trusted bundle signing keys and default public network metadata.

## Scope
- New package `internal/trust`
- constants for default network name + default bundle URL
- trusted key map keyed by `key_id`
- unit tests for expected constants/keys

## Acceptance
- Key lookup by `key_id` works
- No private signing key is committed
- `go test ./...` passes
